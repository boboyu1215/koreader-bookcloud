-- Copy to KOReader's settings/bookcloud.lua while KOReader is stopped.
-- Replace the service address and DEVICE token. Keep /bookcloud, no final slash.
return {
    ["base_url"] = "https://books.example.com/bookcloud",
    ["device_token"] = "REPLACE_WITH_YOUR_DEVICE_TOKEN",
    ["download_dir"] = "/mnt/onboard/book/云书库",
}
